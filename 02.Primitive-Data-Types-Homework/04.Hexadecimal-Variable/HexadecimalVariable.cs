using System;

//Declare an integer variable and assign it with the value 254 in hexadecimal format (0x##). Use Windows Calculator to find
//its hexadecimal representation. Print the variable and ensure that the result is “254”.
class HexadecimalVariable
{
    static void Main()
    {
        int hexNumber = 0xFE; ;
        Console.WriteLine("0xFE in hexadecimal is {0} in decimal format.", hexNumber);
    }
}

